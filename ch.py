import numpy as np
import copy as cp

A = np.random.randint(10, size=(5,5))
Cop=cp.copy(A)
print(A)

def ob(row, column):
	"""mhj"""
	Cop[x,y]=

for x in range(len(A)):
	for y in range(len(x)):
		print(y)
		
		
